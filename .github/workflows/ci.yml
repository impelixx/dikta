name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build

  backend:
    strategy:
      fail-fast: false
      matrix:
        platform: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri
          # prefix-key разово меняет ключ кэша, чтобы этот прогон гарантированно
          # пересобрал sherpa-rs-sys с нуля и заполнил оба кэша (target/ и
          # cache-directories ниже) синхронно — иначе восстановился бы старый
          # "испорченный" target/ с fingerprint, будто библиотеки уже на месте,
          # при пустом ещё cache-directories. Можно убрать после одного зелёного прогона.
          prefix-key: "v2-macos-deploytarget-fix"
          # sherpa-rs-sys скачивает готовые библиотеки sherpa-onnx/onnxruntime
          # не в target/, а в системный кэш (dirs::cache_dir()/"sherpa-rs").
          # rust-cache кэширует только target/ — без этого при восстановлении
          # кэша Cargo видит валидный fingerprint build.rs (значит, повторно
          # его не запускает), но библиотек на свежем раннере физически нет,
          # и линковка падает с "library not found"/cannot open input file.
          cache-directories: |
            ~/Library/Caches/sherpa-rs
            ~/.cache/sherpa-rs
            ~/AppData/Local/sherpa-rs

      - name: Системные зависимости (Linux)
        if: matrix.platform == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf libasound2-dev libxdo-dev

      - name: cargo build
        working-directory: src-tauri
        run: cargo build --locked

      - name: cargo test
        working-directory: src-tauri
        run: cargo test --lib --locked
